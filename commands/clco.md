# clco — Multi-Agent Orchestrator v2

Read and execute the FULL instructions from the orchestrate command file:

```
cat ~/.claude/plugins/codex-orchestrator-v2/commands/orchestrate.md
```

Follow every phase exactly as written in that file. Pass through all user arguments:

Arguments: $ARGUMENTS
